# План улучшений проекта mcp-1c77

## Приоритет 1: Новые MCP-инструменты (неделя 1)

### 1.1 Экспорт конфигурации в JSON
**Файл:** `src/mcp_1c77/tools.py`, `src/mcp_1c77/server.py`
- `export_to_json(output_path: str = "") -> str` — экспорт всей конфигурации в JSON
- `export_object_to_json(object_type: str, name: str) -> str` — экспорт одного объекта

### 1.2 Анализ зависимостей объектов
**Файл:** `src/mcp_1c77/tools.py`
- `get_object_dependencies(object_type: str, name: str) -> str` — какие объекты использует данный
- `find_dependent_objects(object_type: str, name: str) -> str` — какие объекты зависят от данного

### 1.3 Сравнение конфигураций
**Файл:** `src/mcp_1c77/tools.py`, `src/mcp_1c77/comparator.py` (новый)
- `compare_configurations(path1: str, path2: str) -> str` — сравнение двух конфигов
- Показывает добавленные/удалённые/изменённые объекты

## Приоритет 2: Улучшение парсера (неделя 2)

### 2.1 Поддержка общих модулей
**Файл:** `src/mcp_1c77/metadata.py`, `src/mcp_1c77/models.py`
- Парсинг общих модулей (CommonModules)
- Инструмент `list_common_modules()`, `get_common_module(name: str) -> str`

### 2.2 Поддержка прав доступа
**Файл:** `src/mcp_1c77/metadata.py`, `src/mcp_1c77/models.py`
- Парсинг ролей и прав доступа (Roles)
- Инструмент `list_roles()`, `get_role(name: str) -> str`

### 2.3 Многоязычные конфигурации
**Файл:** `src/mcp_1c77/metadata.py`
- Поддержка нескольких языков (синонимы на разных языках)
- Параметр `language` для инструментов

## Приоритет 3: REST API (неделя 3)

### 3.1 REST endpoints
**Файл:** `src/mcp_1c77/web.py`, `src/mcp_1c77/api.py` (новый)
- `GET /api/config` — информация о конфигурации
- `GET /api/objects/{type}` — список объектов
- `GET /api/objects/{type}/{name}` — детально об объекте
- `POST /api/validate/query` — валидация запроса
- `GET /api/export/json` — экспорт в JSON

### 3.2 Кэширование
**Файл:** `src/mcp_1c77/cache.py` (новый)
- LRU-кэш для результатов запросов
- Инвалидация при перезагрузке конфигурации

### 3.3 Аутентификация (опционально)
**Файл:** `src/mcp_1c77/web.py`
- API key authentication
- Переменная окружения `MCP_API_KEY`

## Приоритет 4: CLI утилиты (неделя 4)

### 4.1 CLI команда
**Файл:** `src/mcp_1c77/cli.py` (новый)
- `mcp-1c77 info config.md` — информация о конфиге
- `mcp-1c77 list config.md --type=Документ` — список объектов
- `mcp-1c77 export config.md output.json` — экспорт в JSON
- `mcp-1c77 compare config1.md config2.md` — сравнение

## Приоритет 5: Качество кода (неделя 5)

### 5.1 Типизация
**Файл:** Все файлы
- Добавить type hints везде
- Использовать TypedDict для сложных структур

### 5.2 Тестирование
**Файл:** `tests/`
- Тесты для новых инструментов
- Интеграционные тесты с реальными файлами 1Cv7.MD
- Code coverage > 80%

### 5.3 CI/CD
**Файл:** `.github/workflows/ci.yml` (новый)
- Запуск тестов при push/PR
- Линтинг (ruff, mypy)
- Сборка Docker образа

## Приоритет 6: Производительность (неделя 6)

### 6.1 Lazy loading
**Файл:** `src/mcp_1c77/metadata.py`
- Загрузка модулей и форм по требованию
- Предзагрузка только метаданных объектов

### 6.2 Индексация
**Файл:** `src/mcp_1c77/index.py` (новый)
- Индекс для быстрого поиска по реквизитам
- Индекс для поиска по именам объектов

## Приоритет 7: Документация (неделя 7)

### 7.1 Расширение README
**Файл:** `README.md`
- Примеры использования всех инструментов
- FAQ
- Troubleshooting

### 7.2 API документация
**Файл:** `docs/api.md` (новый)
- OpenAPI спецификация для REST API
- Примеры запросов

## Приоритет 8: Интеграции (неделя 8)

### 8.1 VS Code extension
**Файл:** `extensions/vscode/` (новый)
- Панель для просмотра метаданных
- Быстрый поиск по конфигурации

### 8.2 Графическая визуализация
**Файл:** `src/mcp_1c77/web.py`
- Graphviz интеграция для зависимостей
- Интерактивный граф объектов

---

## План реализации (итеративный подход)

### Итерация 1 (сейчас): Базовые улучшения
1. ✅ Экспорт в JSON
2. ✅ Анализ зависимостей
3. ✅ Улучшение обработки ошибок

### Итерация 2: Расширенный функционал
1. REST API endpoints
2. Кэширование
3. CLI утилиты

### Итерация 3: Продвинутые функции
1. Сравнение конфигураций
2. Общие модули и права доступа
3. Индексация и оптимизация
